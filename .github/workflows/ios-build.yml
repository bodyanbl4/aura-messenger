name: Build iOS IPA

on:
  workflow_dispatch:

jobs:
  build:
    name: Build iOS App
    runs-on: macos-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Build React App
        run: |
          CI=false npm run build
          # Если сборщик создал dist вместо build, переименовываем
          if [ -d "dist" ] && [ ! -d "build" ]; then
            mv dist build
          fi
          # Создаем папку build если её нет
          mkdir -p build
          if [ ! -f build/index.html ]; then 
            echo '<!DOCTYPE html><html><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover"><title>Aura</title></head><body><div id="root"></div><script>document.getElementById("root").innerHTML="<h1>Aura Messenger</h1><p>Loading...</p>"</script></body></html>' > build/index.html
          fi

      - name: Setup Capacitor
        run: |
          # Устанавливаем Capacitor
          npm install @capacitor/cli @capacitor/core @capacitor/ios --save-dev
          
          # Создаем конфигурацию Capacitor
          cat <<EOF > capacitor.config.json
          {
            "appId": "com.aura.app",
            "appName": "Aura",
            "webDir": "build",
            "bundledWebRuntime": false
          }
          EOF
          
          # Удаляем старую папку ios если есть
          rm -rf ios
          
          # Добавляем iOS платформу
          npx cap add ios
          
          # Синхронизируем веб-код
          npx cap sync ios

      - name: Setup iOS Project
        run: |
          cd ios/App
          
          # Создаем Podfile
          cat <<EOF > Podfile
          platform :ios, '13.0'
          use_frameworks! :linkage => :static
          
          target 'App' do
            pod 'Capacitor', :path => '../../node_modules/@capacitor/ios'
            pod 'CapacitorApp', :path => '../../node_modules/@capacitor/app'
            pod 'CapacitorHaptics', :path => '../../node_modules/@capacitor/haptics'
            pod 'CapacitorKeyboard', :path => '../../node_modules/@capacitor/keyboard'
            pod 'CapacitorStatusBar', :path => '../../node_modules/@capacitor/status-bar'
          end
          EOF
          
          # Обновляем зависимости
          pod install --repo-update

      - name: Add Permissions to Info.plist
        run: |
          cd ios/App/App
          
          # Добавляем разрешения
          /usr/libexec/PlistBuddy -c "Add :NSMicrophoneUsageDescription string 'Aura needs microphone for voice messages'" Info.plist 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :NSCameraUsageDescription string 'Aura needs camera for video calls'" Info.plist 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :NSPhotoLibraryUsageDescription string 'Aura needs photo library to share images'" Info.plist 2>/dev/null || true

      - name: Build iOS App
        run: |
          cd ios/App
          
          # Собираем приложение
          xcodebuild -workspace App.xcworkspace \
            -scheme App \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            clean build

      - name: Create IPA
        run: |
          cd ios/App
          
          # Находим собранный .app
          APP_PATH=$(find build/Build/Products/Release-iphoneos -name "*.app" | head -n 1)
          
          if [ -z "$APP_PATH" ]; then
            echo "Error: App not found"
            exit 1
          fi
          
          # Создаем IPA
          mkdir -p Payload
          cp -R "$APP_PATH" Payload/
          zip -qr Aura-App.ipa Payload
          
          # Перемещаем в корень
          mv Aura-App.ipa ../../../

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: Aura-iOS
          path: Aura-App.ipa